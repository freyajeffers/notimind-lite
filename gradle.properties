org.gradle.jvmargs=-Xmx6g -XX:+UseParallelGC -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=true
org.gradle.vfs.watch=true
android.nonTransitiveRClass=true
kotlin.code.style=official
org.gradle.workers.max=8
kotlin.compiler.execution.strategy=daemon
ksp.incremental=true
kotlin.incremental=true
# FIXME: AGP-10 migration — these flags are deprecated in AGP 10 (default is true)
# but kept today as a workaround for the Kotlin 2.2.10 ↔ AGP 9.1.1
# KotlinAndroidPluginWrapper.kt:34 / createKotlinExtension double-registration
# collision (KT-76442-class). Re-evaluate on Kotlin bump past the fix.
android.builtInKotlin=false
android.newDsl=false
