<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Main Theme Tokens -->
    <style name="Theme.NotiMindLite" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorTertiary">@color/tertiary</item>
        <item name="android:windowBackground">@color/background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>

    <!-- Accessibility and Contrast Overrides -->
    <style name="Widget.NotiMindLite.Button" parent="Widget.Material3.Button">
        <item name="android:textAppearance">@style/TextAppearance.NotiMindLite.Button</item>
        <item name="android:minHeight">48dp</item>
    </style>

    <style name="TextAppearance.NotiMindLite.Button" parent="TextAppearance.Material3.LabelLarge">
        <item name="android:textStyle">bold</item>
        <item name="android:letterSpacing">0.02</item>
    </style>
</resources>
